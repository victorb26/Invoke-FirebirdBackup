# Invoke-FirebirdBackup
Script desenvolvido em PowerShell para automatizar a rotina de backup de bancos de dados Firebird (.fdb/.fbk) em ambientes Windows Server. O objetivo principal é garantir a integridade dos dados e a redundância em nuvem (OneDrive/Azure).
